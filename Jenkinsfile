pipeline {
  agent { dockerfile true }
    stages {
        stage('Test') {
            steps {
                //withRun('-p 8080:80')
                sh 'echo dockerfile' 
                
            }
        }
    }
}
